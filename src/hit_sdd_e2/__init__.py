"""hit-sdd-bench-e2: brownfield acceptance-feedback ablation harness (Python/Docker).

Implements the E2 Phase-1 pilot specified in the sibling record repo `hit-sdd-bench`
(`docs/protocols/e2-phase1-pilot-spec-v1.md`). This package holds harness CODE only; the
scientific record (design boundary, pilot spec, commitments docs, run-cards, evidence)
lives in `hit-sdd-bench`.
"""
